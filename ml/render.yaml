services:
  - type: web
    name: casinozz-ml
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:5001 --timeout 120 --workers 2
    envVars:
      - key: DATABASE_URL
        sync: false
